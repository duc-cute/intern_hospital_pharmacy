export class Lot {
    source = null; // nguồn thuốc
    batchCode = null; // Mã lô
    product = null;
    stockKeepingUnit = null;
    quantity = null; // số lượng ban dau
    quantityFirstImport = 0; // so luong da nhap kho trung uong
    quantityExport = 0; // so luong da cap phat su dung
    price = null; // đơn giá
    amount = null; // tổng tiền
    coefficient = null;// Trọng số : Ví dụ 1 vỉ thuốc có 10 viên => trọng số bằng 10 nếu đơn vị là vỉ.
    // Nếu đơn vị là viên thì trọng số =1
    expiryDate = null; // hạn sử dụng
    skus = [];
    defaultSku = null;
    orderNumber = null;
    productSkuName = null;
    supplier = null;    // nhà cung cấp
    biddingPackage = null;    // gói thầu
    dateTime = null;    // ngay tao moi
}
